const ytdl = require('ytdl-core');
const { VoiceChannel } = require('discord.js');

/**
 * 
 * @param {String} id 
 * @param {VoiceChannel} voiceChannel Voice Channel to play music in
 */
async function playMusic(id, voiceChannel) {

}